{
  "_comment": "Gemini CLI MCP config. Merge the mcpServers block into ~/.gemini/settings.json (or .gemini/settings.json in your project). Replace the command path and token. The token is read from env, never from prompts.",
  "mcpServers": {
    "runtz": {
      "command": "/usr/local/bin/runtz-mcp",
      "args": [],
      "env": {
        "RUNTZ_ENDPOINT": "https://engine.runtz.dev",
        "RUNTZ_TOKEN": "rtz_live_replace_me",
        "RUNTZ_MCP_BIN": "runtz"
      }
    }
  }
}
