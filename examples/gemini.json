{
  "_comment": "Gemini CLI MCP config. Merge the mcpServers block into ~/.gemini/settings.json (or .gemini/settings.json in your project). Nothing to install — this points at the runtz-hosted docs server.",
  "mcpServers": {
    "runtz": {
      "httpUrl": "https://mcp.runtz.dev/mcp"
    }
  }
}
