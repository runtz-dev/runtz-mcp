{
  "$comment": "Claude Code / Claude Desktop MCP config. For Claude Code, save this as .mcp.json in your project root (or merge the mcpServers block into ~/.claude.json). For Claude Desktop, merge into claude_desktop_config.json. Nothing to install — this points at the runtz-hosted docs server.",
  "mcpServers": {
    "runtz": {
      "type": "http",
      "url": "https://mcp.runtz.dev/mcp"
    }
  }
}
