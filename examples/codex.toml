# OpenAI Codex MCP config.
# Merge this into ~/.codex/config.toml. Nothing to install — this points at
# the runtz-hosted docs server over streamable HTTP.

[mcp_servers.runtz]
type = "http"
url = "https://mcp.runtz.dev/mcp"
