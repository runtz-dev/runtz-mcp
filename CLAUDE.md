See [AGENTS.md](AGENTS.md) for the repository layout, the single HTTP
docs-only transport, the dev→main branch flow, the checks to run before a
pull request, and the hard rules (no secrets in the repo; keep the server
docs-only, don't reintroduce scan execution).
