See [AGENTS.md](AGENTS.md) for the repository layout, the two transports (stdio
with scans, HTTP docs-only), the dev→main branch flow, the checks to run
before a pull request, and the hard rules (no secrets in the repo; keep the HTTP
transport docs-only).
